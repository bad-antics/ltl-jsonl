// examples/demo.ltl — runnable demo for ltl_jsonl

import std::io
use ltl_jsonl as lib

fn main() {
    io::println("ltl_jsonl demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}